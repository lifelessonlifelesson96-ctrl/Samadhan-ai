<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width">
  <title>Samadhan AI — समाधान</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: sans-serif; background: #f5f5f5;
           display: flex; flex-direction: column;
           min-height: 100vh; }
    .header { background: #fff; padding: 14px 20px;
              border-bottom: 1px solid #eee;
              display: flex; align-items: center; gap: 10px; }
    .logo { width: 32px; height: 32px; background: #3b5bdb;
            border-radius: 8px; display: flex; align-items: center;
            justify-content: center; color: #fff;
            font-weight: 700; font-size: 14px; }
    .app-title { font-size: 16px; font-weight: 600; }
    .chat { flex: 1; padding: 16px; display: flex;
            flex-direction: column; gap: 12px;
            max-width: 700px; width: 100%; margin: 0 auto; }
    .bubble { padding: 10px 14px; border-radius: 12px;
              font-size: 14px; line-height: 1.6;
              max-width: 85%; }
    .bot { background: #fff; border: 1px solid #e8e8e8;
           align-self: flex-start; }
    .user { background: #3b5bdb; color: #fff;
            align-self: flex-end; }
    .input-area { background: #fff; border-top: 1px solid #eee;
                  padding: 12px 16px; display: flex;
                  gap: 10px; max-width: 700px;
                  width: 100%; margin: 0 auto; }
    input { flex: 1; padding: 10px 14px; border-radius: 10px;
            border: 1px solid #ddd; font-size: 14px; }
    button { padding: 10px 20px; background: #3b5bdb;
             color: #fff; border: none; border-radius: 10px;
             cursor: pointer; font-size: 14px; }
  </style>
</head>
<body>
<div class="header">
  <div class="logo">S</div>
  <div class="app-title">Samadhan AI — समाधान</div>
</div>
<div class="chat" id="chat">
  <div class="bubble bot">
    Namaste! Tell me your problem.
    I will help you solve it.
  </div>
</div>
<div class="input-area">
  <input id="inp" placeholder="Type your problem..."
    onkeydown="if(event.key==='Enter')send()">
  <button onclick="send()">Send</button>
</div>
<script>
const API_KEY = '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';

async function send() {
  const inp = document.getElementById('inp');
  const msg = inp.value.trim();
  if (!msg) return;
  inp.value = '';

  addBubble(msg, 'user');
  const loading = addBubble('Thinking...', 'bot');

  const res = await fetch(
    'https://api.anthropic.com/v1/messages',
    {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model: 'claude-sonnet-4-20250514',
        max_tokens: 1000,
        system: `You are Samadhan AI, a helpful
          Indian problem-solving assistant.
          Give clear step-by-step answers in
          simple language. Keep under 100 words.`,
        messages: [{ role: 'user', content: msg }]
      })
    }
  );
  const data = await res.json();
  loading.textContent =
    data.content?.[0]?.text || 'Sorry, try again.';
}

function addBubble(text, type) {
  const chat = document.getElementById('chat');
  const b = document.createElement('div');
  b.className = 'bubble ' + type;
  b.textContent = text;
  chat.appendChild(b);
  chat.scrollTop = chat.scrollHeight;
  return b;
}
</script>
</body>
</html>
